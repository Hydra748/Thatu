# Thatu
A software capable to detect vibrations and taps, and can be used as an external physical trigger to open apps on the desktop.
Though a prototype, the current version encompasses audio detection and uses frequency analysis and filtering to detect and open app(Google) in this case.
Further on the laptops supporting inbulit vibration sensor, vibration detection is also added.
